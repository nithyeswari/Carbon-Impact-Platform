{
    "name": "carbon-impact-platform",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "docker-compose up",
        "build": "lerna run build",
        "start": "lerna run start",
        "test": "lerna run test"
    },
    "devDependencies": {
        "eslint": "^8.0.0",
        "lerna": "^6.0.0",
        "prettier": "^2.8.0",
        "typescript": "^4.9.0"
    },
    "dependencies": {
        "lucide-react": "^0.460.0"
    }
}
